spring:
  application:
    name: customer-service
  cloud:
    consul:
      discovery:
        instance-id: ${spring.application.name}:${random.value}
      config:
        enabled: false
      host: localhost
      port: 8500